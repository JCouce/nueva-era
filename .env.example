# Postgres. Local con Docker usa la línea de abajo; en prod apunta a Neon.
DATABASE_URL="postgresql://rol:rol@localhost:5433/nuevaera?schema=public"

# Secreto de sesión. Genera uno con: openssl rand -base64 33
AUTH_SECRET=""

# Google OAuth (opcional, se añade más adelante)
# AUTH_GOOGLE_ID=""
# AUTH_GOOGLE_SECRET=""
